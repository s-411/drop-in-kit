# BUILD_PLAN.md Template

Copy this template and fill it in based on the app analysis. Remove any sections that don't apply.

---

```markdown
# BUILD_PLAN.md
> Generated by app-backend-builder skill
> DO NOT edit this file manually — hand it to a fresh Claude Code agent to execute

## App Summary
- **App:** [name]
- **For:** [target user]
- **Core problem:** [one sentence]
- **What exists:** [onboarding screens, paywall, auth — list what's already built]
- **What this plan builds:** [the actual app — core loop, navigation, screens, data]

## Paywall Status
- Paywall screen exists: YES / NO
- Paywall bypassed for MVP: YES — [explain what was changed, e.g. "Removed paywall from onboarding navigator, added direct route to MainApp"]
- To re-enable payments later: [exact steps to restore — e.g. "Re-add PaywallScreen to OnboardingNavigator after SubscriptionScreen in App.tsx line X"]

## Tech Stack
- Navigation: [library]
- State: [library or none]
- Data storage: [AsyncStorage / Supabase / Firebase]
- Notifications: [expo-notifications — needs setup / already configured]

## Bottom Navigation
| Tab | Screen | Icon |
|-----|--------|------|
| [Tab 1] | [ScreenName] | [icon name] |
| [Tab 2] | [ScreenName] | [icon name] |
| [Tab 3] | [ScreenName] | [icon name] |
| [Tab 4] | [ScreenName] | [icon name] |

## Data Model
[List every data entity, its fields, and where it's stored]

### [Entity 1]
Stored in: AsyncStorage / Supabase table `[name]`
Fields:
- `id` — string (uuid)
- `[field]` — [type] — [purpose]

---

## Phase 1 — Navigation Shell
**Goal:** Get the bottom tab navigator working with placeholder screens for every tab.

Tasks:
- [ ] Create `MainNavigator.tsx` with bottom tab navigator
- [ ] Create placeholder screen for each tab: `[Screen1].tsx`, `[Screen2].tsx`, etc.
- [ ] Wire onboarding completion to navigate to `MainNavigator`
- [ ] Confirm paywall bypass is in place

**Verification:**
- App launches, completes onboarding, lands on the home tab
- All tabs are tappable and navigate to their placeholder screen
- No console errors
- Run self-check skill

---

## Phase 2 — Data Layer
**Goal:** Set up data storage so screens have something to read and write.

Tasks:
- [ ] Create `storage.ts` (or equivalent) with helper functions for AsyncStorage reads/writes
- [ ] Define data schema for: [list entities]
- [ ] Create initial seed/default data for first launch
- [ ] Write and test: `saveEntry()`, `getEntries()`, `getUserPreferences()`, `saveUserPreferences()`

**Verification:**
- Can write and read back a test entry without errors
- First launch creates correct default state
- Run self-check skill

---

## Phase 3 — Home / Dashboard Screen
**Goal:** Build the main screen the user sees every time they open the app.

Screen: `HomeScreen.tsx`

UI elements:
- [Greeting / status message]
- [Streak counter]
- [Primary CTA button — navigates to core action]
- [Summary of today's progress]
- [Empty state if no data yet]

Data needed: [list what it reads from storage]

**Verification:**
- Home screen renders with real data from storage
- Empty state shows correctly on first launch
- CTA button navigates to correct screen
- Run self-check skill

---

## Phase 4 — Core Action Screen
**Goal:** Build the primary screen where the user does the main thing.

Screen: `[CoreScreen].tsx`

UI elements:
- [Describe the main input/action]
- [Submit / save button]
- [Confirmation feedback]

On save: writes to storage, updates streak if applicable, navigates back to Home

**Verification:**
- User can complete the core action
- Data is saved and persists after app restart
- Home screen reflects the new data
- Run self-check skill

---

## Phase 5 — Progress / History Screen
**Goal:** Show the user their history so the app feels useful.

Screen: `ProgressScreen.tsx`

UI elements:
- [List or calendar of past entries]
- [Streak display]
- [Simple stat: total entries, days active, etc.]
- [Empty state]

Data needed: all stored entries, sorted by date descending

**Verification:**
- History shows real entries
- Empty state shows when no entries exist
- Run self-check skill

---

## Phase 6 — Settings Screen
**Goal:** Let the user configure the app and manage notifications.

Screen: `SettingsScreen.tsx`

UI elements:
- Notification toggle (on/off)
- Notification time picker
- [Any app-specific settings, e.g. weekly goal, drink limit]
- App version (read from app.json)
- [Optional: reset data / log out]

On save: writes preferences to storage, reschedules notifications if time changed

**Verification:**
- Settings save and persist
- Notification toggle works
- Run self-check skill

---

## Phase 7 — Push Notifications (if applicable)
**Goal:** Set up daily reminders.

Tasks:
- [ ] Request notification permissions on first launch (after onboarding)
- [ ] Schedule daily local notification at user's preferred time
- [ ] Cancel and reschedule when user changes time in Settings
- [ ] Test notification fires correctly

**Verification:**
- Permission prompt appears after onboarding
- Notification appears at scheduled time in simulator
- Changing time in Settings updates the scheduled notification
- Run self-check skill

---

## Phase 8 — Polish & Wiring Check
**Goal:** Make sure everything is connected before App Store submission.

Tasks:
- [ ] Invoke screen-wiring skill — fix any disconnected screens
- [ ] Invoke design-consistency skill — fix any visual drift
- [ ] Check all empty states exist
- [ ] Check all loading states exist
- [ ] Remove any console.log statements
- [ ] Remove any placeholder/hardcoded text
- [ ] Run self-check skill

Then invoke app-store-approval skill.

---

## Out of Scope (Add to LATER.md)
- Payments / subscriptions
- Social features
- Advanced analytics
- [Anything else identified but cut]
```
